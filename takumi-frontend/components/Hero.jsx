import HeroCarousel from "./HeroCarousel"

const Hero = () => {
    return (
        <section className="py-0 h-full relative overflow-hidden">
            <div>
                <HeroCarousel/>
            </div>
        </section>
    )
}

export default Hero